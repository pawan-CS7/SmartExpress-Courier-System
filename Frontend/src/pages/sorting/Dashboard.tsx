import React from "react";

const Dashboard: React.FC = () => {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold">Warehouse Dashboard</h1>
      <p>Active scan rates, recent scans, and packages currently at the Warehouse.</p>
    </div>
  );
};

export default Dashboard;
